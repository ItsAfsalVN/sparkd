import 'package:flutter/material.dart';

class SparkDashboard extends StatelessWidget {
  const SparkDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text("Spark Dashboard"));
  }
}

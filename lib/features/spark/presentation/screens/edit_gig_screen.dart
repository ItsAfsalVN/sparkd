import 'package:flutter/material.dart';

class EditGigScreen extends StatelessWidget {
  const EditGigScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Edit Gig')),
      body: const Center(child: Text('Edit Gig Screen Content')),
    );
  }
}

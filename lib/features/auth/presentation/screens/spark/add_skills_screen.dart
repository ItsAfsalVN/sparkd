import 'package:flutter/material.dart';

class AddSkillsScreen extends StatelessWidget {
  const AddSkillsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Add skills'),),
    );
  }
}